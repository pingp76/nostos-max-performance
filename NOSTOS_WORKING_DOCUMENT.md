# 《Nostos：一句话的返航》工作文档

> 当前版本：v0.33
> 更新日期：2026-09-02
> 性质：本项目的持续更新工作文档。已经形成阶段性共识时，应先更新本文档，再继续设计或实现。

## 1. 一周目标

在一周内完成一个能够稳定演示或现场表演的第一版：

- 总时长约 4 分钟，排练上限为 4分15秒；其中 Stage 1 计时器启动前的录音与首次原句播放约20—30秒。
- 使用 Max/MSP 完成，不使用 Faust。
- 使用 Mac 运行 Max，使用 iPad TouchOSC 进行主要表演控制。
- 现场录制观众朗读的一句话，整句话作为唯一语音来源。
- 不再把语音分成神界、人界、冥界三段，也暂不制作三个彼此独立的声音世界。
- 作品经历三个阶段：出发／战争、破碎／漂流、重组／归返。
- 每个阶段都有一层持续播放的底音；录音原句或其切片在底音之上发生变化。
- 第一版不再加入摄像头或 X/Y 手势控制，把操作集中到少量声音宏观参数与事件按钮。

第一版的成功标准不是系统功能丰富，而是观众能够听见：

> 一句话离开说话者，经历冲突与破碎，最后重新获得语言秩序并返回。

## 2. 当前核心叙事

作品暂时不再强调三界并行，而把《奥德赛》作为一条时间旅程：

1. **出发／战争**：句子仍然完整，但被推动、重复和逆转，逐渐脱离说话者。
2. **破碎／漂流**：句子失去连续性，只剩切片、残片和偶尔能够辨认的词。
3. **重组／归返**：碎片逐渐恢复正确关系，语言可辨认度增加，最终完整返回。

贯穿全曲的主要作曲参数仍然是语言可辨认度：

> 完整清晰 → 受到干扰 → 破碎 → 偶尔认出词语 → 逐渐恢复 → 完整返回

### 2.1 可表演性原则（已确定）

声音设计必须让观众听见演奏动作，而不只是让系统内部参数发生变化：

- 推子移动时，应在短时间内听见连续而明显的变化；避免大段无效区间或只有极端位置才有作用。
- 打击垫按下时，应立即产生清楚的声音事件，例如倒放冲击、切片爆发、突然中断或清晰词召回。
- 主要表演动作的听觉反馈原则上应在约 0–500 ms 内开始出现。
- 较长的渐变可以存在，但应由场景自动运行，或者先产生一个明确的起始变化；不能让演奏者操作后长时间听不出结果。
- 每个控制在排练前都要通过“看不到界面时，能否仅凭听觉判断它被操作”这一测试。
- 一个效果如果必须精细调整许多数值才能被听见，第一版优先删除或改成一个宏观控制。

## 3. 已确定的四分钟时间结构

| 时间 | 段落 | 底音职责 | 录音材料职责 | 表演目标 |
| --- | --- | --- | --- | --- |
| 计时前约0:00–0:25 | 录音与记忆建立 | 无 | 现场录音；完整原句干声播放一次；结尾留白约2秒 | 让观众记住原句 |
| 屏幕0:00–0:50 | 第一阶段：出发／战争 | 稳定、带方向感，随DENSITY增强 | TRANSFORM渐变、一次倒放事件和一次密度推进 | 从完整走向冲突，但仍可辨认 |
| 屏幕0:50–2:05 | 第二阶段：破碎／漂流 | 割裂金属；包含纯背景留白和金属碎屑事件 | 密度上升、粒子风暴、短时HOLD、人声下沉与一次清晰片段召回 | 展示语言真正破碎、冻结和被记忆召回 |
| 屏幕2:05–3:25 | 第三阶段：重组／归返 | 风沙、沙浪和短时BED遮蔽逐渐让出空间 | 逐渐显现事件、由85回到0的TRANSFORM长动作和VOICE退场 | 让恢复秩序本身可被听见 |
| 屏幕3:25–约3:45 | 返回与余波 | 场景淡出 | 完整原句返回；结束后安静与总音量淡出 | 回答“回来后是否仍是原句” |

计时器内约3分40—50秒，加上计时前奏后整场约4—4分15秒。增加的动作各自展示一种明显效果，但段落间仍保留正式留白。

### 3.1 演出系统架构（已确定）

- 正式演出只打开一个主 Patch，不在演出中切换三个独立文件或窗口。
- 主 Patch 内含三个独立的场景声音模块：第一阶段、第二阶段、第三阶段。
- 三个模块共享同一个现场录音 `buffer~`；各自使用播放器和处理链读取同一份语音材料。
- 三个模块的输出分别经过独立的 `line~`＋`*~` 场景增益，再汇合到唯一的总输出安全链。
- 按下 `STAGE 1/2/3` 时切换的是内部场景状态，不是 Patch 文件。系统按固定顺序停止旧事件、初始化新场景并开始交叉淡化。
- 每次进入 Stage 都自动召回该 Stage 的可预测初始参数，并把数值同步显示在推子上；录音 buffer、MASTER、MUTE 和 DSP 状态不随 Stage 改变。
- 场景按钮操作后立即开始变化；第一版交叉淡化暂从约 `300–800 ms` 测试，之后按听觉需要调整。
- 第一版可以把三个阶段平铺在同一主 Patch 中；模块稳定后允许收进三个命名子 Patch，但演出仍只打开一个主文件。
- 声音实验文件可以分别保留，不作为正式演出时需要同时操作的文件。

工程视图从上到下划分为：

1. 公共录音、共享 buffer、原句播放与 MCP 运行时工具。
2. 场景状态、宏观控件、演出遥控、Stage 预设和计时器。
3. 第一阶段：出发／战争。
4. 第二阶段：破碎／漂流。
5. 第三阶段：重组／归返。
6. 场景混音、主增益、音量表、限制器、紧急静音和扬声器输出。
7. TouchOSC UDP 与地址路由等低频配置。

演出使用单页 Presentation Mode，只显示必要按钮、推子、打击垫和安全控制，不显示内部连线或 X/Y。

Presentation Mode 上方提供醒目的 `MM:SS` 演出计时器：进入 Stage 1 时自动从 `00:00` 开始并持续跨越 Stage 2、Stage 3 和 RETURN；RESET 会停止计时并清零。录音和首次原句播放属于计时器启动前的前奏。

## 4. 三阶段的最小声音设计

### 4.1 第一阶段：出发／战争

听觉目标：不是写实战争音效，而是让完整语言受到推动、争夺和阻力。

- 底音：稳定的低层或脉冲，提供前进感；最多一至两层。
- 语音：先完整正向重复，再加入错时分身、倒放或速度变化。
- 密度：由稀到密。
- 可辨认度：高 → 中等。
- 表演动作：启动段落、提高冲突强度、触发一次明显的倒放事件。

已试听成果 A：倒放会让语言脱离正常时间方向，可以作为“离开”“命运回卷”或战争中秩序被扭转的事件。第一版保留。

### 4.2 第二阶段：破碎／漂流

听觉目标：语言失去句法连续性，但观众仍偶尔记起原句。

- 底音：稀疏、悬浮或不稳定，避免持续占满中频。
- 语音：使用同一 buffer 的短切片；改变切片长度、触发间隔和位置。
- 密度：允许在稀疏与短时密集之间变化。
- 可辨认度：低，但每隔一段时间应出现一个较清楚的词或较长片段。
- TouchOSC 负责：切片密度、破碎程度、语音与底音比例，以及显式事件触发。
- 第一版不提供手势输入；空间与音色的细微运动由系统内部受约束地完成。

第一版只选择一种切片机制，不同时开发多套粒子、频谱或复杂随机系统。

本轮试听后已确认，Stage 2 必须和 Stage 3 一耳区分；v0.19 已按以下方向完成改造，仍需在 Max 中试听验收：

- Stage 2 背景改成“破碎金属”：两组不同周期的短脉冲分别激发约 `1370 Hz` 与 `2390 Hz` 的共振，经过限幅后进入 BED；它不是连续风声，而是一组间歇、硬质、彼此错开的金属残片。
- `DENSITY` 同时控制金属脉冲间隔，约由 `900–110 ms` 映射；推高时人声粒子与金属底层会一起变得更密集。
- `DENSITY` 改为真正明显的事件密度：低位是零散、较长的语音残片，高位是快速、短小、可重叠的碎片云；有效范围内持续可听，不再只改变一个不明显的 metro 数值。
- `TRANSFORM` 同时控制粒子长度和滤波暗度：低位保留较长、较清楚的语言残片，高位变成更短、更暗的碎片，使它仍是最强的变形轴。
- `VOICE` 使用更大的可听动态范围：0 必须只剩背景，100 必须让语音／粒子明显压到前景；仍与 BED 保持独立。
- `EVENT A` 触发一次约 1.2 秒的粒子风暴，`EVENT B` 继续召回较清楚的片段。
- Stage 2 的 `BED PARTICLE` 是独立于录音的金属事件：每次触发产生约 0.5 秒的多次金属碎屑爆裂；即使 `VOICE=0`、`AUTO VOICE=0` 也能使用，整体音量仍由 `BED` 控制。

### 4.3 第三阶段：重组／归返

听觉目标：不能突然切回原录音，必须听见秩序逐步恢复。

- 底音：逐渐稳定，并在完整原句返回前让出空间。
- 语音重组顺序：清晰词稳定出现 → 相邻片段顺序恢复 → 片段间隔缩短 → 完整原句返回。
- 可辨认度：低 → 高 → 完整。
- 可复用已试听成果 B：“逐渐显现”滤波可以用于归返阶段，让语言从遮蔽状态缓慢变清楚。
- B 区参数含义：参数1为目标截止频率，参数2为共振强度，参数3为显现时间。
- 已确认的神谕体验示例：`4800 Hz / 0.85 / 15000 ms`，表现为长时间含混后带强烈意志地显现。用于归返时可降低共振，避免结尾过于尖锐。
- 上述 15 秒变化适合预先编排的段落过程，不适合作为主要手动表演反馈。若由打击垫触发，第一版应把显现时间缩短到约 `1500–5000 ms`，并让按下瞬间产生可听见的暗化、强调或起始冲击。

Stage 3 已确定保留较连续的“风吹沙／归返坐标”感，与 Stage 2 的硬质金属形成对比。v0.19 的结构是：

- 保留低音基准、缓慢呼吸和轻微拍频，并增加一层宽频、缓慢起伏的风沙噪声。
- 高 `TRANSFORM` 时背景仍有漂移与摩擦；向 0 移动时频率关系逐渐稳定、噪声退去，让“秩序恢复”同时发生在背景与语言中。
- 第一版不把 Stage 2 粒子层复制到 Stage 3，以降低 CPU、连线和排练复杂度；Stage 3 继续使用连续语音，背景中的残余噪声负责连接漂流与归返。
- Stage 3 的 `BED PARTICLE` 使用同一表演按钮，但声音变为约 1.5 秒的沙浪掠过；它与 Stage 2 的短金属爆裂共享控制入口，不共享声音链。

### 4.4 粒子效果方案（A 已确定并实现，待试听）

Max 可以使用多个读取同一 `buffer~` 的播放声部制作粒子效果。第一版不增加新的 TouchOSC 推子，复用现有三个宏观参数：

- `DENSITY`：每秒粒子数量／触发间隔。
- `TRANSFORM`：粒子长度、速度偏移和语言破碎程度。
- `VOICE`：整层粒子云的前景音量。

本轮曾比较三个强度选项：

- A｜受控微粒：约 4 个重叠声部，仍能偶尔认出语音，最适合一周原型。
- B｜强颗粒云：约 8—12 个声部，更猛烈但更容易糊成一团，音量和 CPU 风险更高。
- C｜强化现有切片：不做真正重叠声部，只扩大触发与长度范围；最稳定，但“粒子感”较弱。

已选择 A。主 Patch 使用 `poly~ Nostos_GrainVoice 4 @steal 1` 轮询四个粒子声部；粒子长度约由 `45–750 ms` 映射，自动触发间隔约由 `1600–70 ms` 映射。每个粒子带 `10 ms` 淡入、`15 ms` 淡出和固定声部安全增益。`poly~` 会自动加载 `Nostos_GrainVoice.maxpat`；正式演出仍只需要打开主 Patch，但两个文件必须保存在同一项目文件夹。

## 5. TouchOSC 第一版控制范围

为了降低新手演出复杂度，第一版界面暂定只使用一个主要页面，并只保留必要控制。不同阶段尽量复用同一组推子和打击垫，由当前场景决定其作用，不为每个效果增加独立组件。

### 必要按钮

- RECORD：开始／停止录音。
- PLAY ORIGINAL：完整播放原句。
- STAGE 1：进入出发／战争。
- STAGE 2：进入破碎／漂流。
- STAGE 3：进入重组／归返。
- END / RETURN：触发最终完整返回。
- MUTE：紧急静音。
- AUTO VOICE：开启／关闭当前 Stage 的自动语音流；关闭时背景继续，EVENT A／B／HOLD仍可手动发声，RETURN始终有效。
- BED PARTICLE：触发一次不含人声的背景事件；Stage 2 是短金属爆裂，Stage 3 是较长沙浪。
- RESET：回到待机场景并恢复默认参数、MASTER、MUTE 和 HOLD，同时停止正在播放的原句；不清除录音 buffer，也不关闭 DSP。

### 必要状态显示

- 演出计时器：只读 `MM:SS` 显示；Stage 1 自动开始，普通换段不重置，RESET 停止并回到 `00:00`。

### 必要连续控制

- MASTER：总音量。
- VOICE：录音材料音量。
- BED：当前 Stage 背景层音量，与VOICE完全分离。
- TRANSFORM：当前阶段的总体变形强度。
- DENSITY：重复或切片事件密度。

BED和VOICE均保留为演出推子，使背景与前景能够分开塑形；背景具体效果未来收进少量宏观控制，不为每个底层参数增加界面组件。

### 表演打击垫

主页面保留少量可复用打击垫：

- EVENT A：当前阶段的主要强事件，例如倒放冲击或切片爆发。
- EVENT B：当前阶段的对比事件，例如突然中断或清晰词召回。
- HOLD（可选）：按住期间维持冻结、重复或空间悬停，松手立即恢复。
- BED PARTICLE：不读取语音 buffer；在 Stage 2 产生金属碎屑，在 Stage 3 产生风沙浪，可在关闭自动语音后使用。

每个打击垫必须在按下瞬间产生明显结果，并有安全的音量和最长持续时间。

### TouchOSC 当前实现

- 公开版本只提供 `nostos_full.tosc`：`640×1160` 单页双向界面，包含录音／播放、场景、事件、五个宏观控制、MASTER、HOLD、DSP、SYNC、当前场景、计时器、MASTER dB和反馈在线状态。
- 布局使用TouchOSC `lexml version='6'`，所有控件名称和ID稳定且唯一。`tools/generate_nostos_touchosc_full.js`可在现有完整布局中更新根级反馈脚本；最终分发资产以仓库根目录的 `nostos_full.tosc` 为准。
- Stage按钮仍会先在TouchOSC本地显示对应预设，再发送 `/nostos/stage 1..4`。完全版收到Max状态时，以Max返回值更新TRANSFORM、DENSITY、VOICE、BED、AUTO、HOLD、MUTE、RECORD、DSP、MASTER、场景和计时器。
- 完全版根级Lua使用 `onReceiveOSC` 处理 `/nostos/state/...`；程序回填控件时不再次向Max发送，避免双向反馈循环。用户触摸控件时才发送 `/nostos/...`。
- 主Patch已加入命名子Patch `p touchosc_feedback`：缓存12类运行状态，参数改变时立即发送，并在收到 `/nostos/sync 1` 后重发全部当前值。计时器按整数秒去重后回传，DSP状态由Max定期查询。
- 场景编号与计时秒数另通过 `/nostos/state/transport <stage> <seconds>` 每500 ms成对回传。TouchOSC用同一个快照同时刷新顶部场景说明、状态区场景标签和计时器；单个UDP包丢失后会在下一次心跳自动恢复。
- TouchOSC Connection 1需要在设备上手动配置：UDP、Host为Mac局域网IP、Send Port为9000（TouchOSC→Max）、Receive Port为9001（Max→TouchOSC）。连接设置不写入布局文件。
- Max工程视图底部提供可在锁定Patch中直接编辑的 `textedit` 回传地址输入，默认 `127.0.0.1`；输入iPad局域网IP后按Enter或点击“应用IP”。TouchOSC随后点击SYNC，反馈状态应从WAITING变为ONLINE。
- Max输入继续使用 `udpreceive 9000 → route /nostos/...`，无需 `oscparse`。完全版和9001回传均已通过静态结构检查，仍须重开Max并在iPad上完成运行时验收。
- TouchOSC的简单数值发送会把按钮和阶段编号编码为OSC浮点数。Max输入端现将STAGE、EVENT、RECORD、PLAY、MUTE、HOLD、RESET、AUTO、BED PARTICLE、DSP和SYNC统一经 `int` 规格化后再进入离散分支；TRANSFORM、DENSITY、VOICE、BED和MASTER等连续参数仍保持浮点。
- HOLD从TouchOSC进入与Max界面共用的 `ui_hold_raw` 入口，再由既有链路生成 `ctl_hold` 和状态回传。HOLD的声音职责目前只在Stage 2生效。

### 手势映射

本轮试听已确定：第一版取消 `X` 左右声像和 `Y` 暗亮两个表演效果。v0.17 已从主界面、Stage 预设、声音映射和操作谱中删除相关结构。摄像头手势延后到未来版本，不再占用本周的设计、排练和界面复杂度。

## 6. 系统与表演分工

| 内容 | 控制方 |
| --- | --- |
| 录音、换段、最终返回、紧急静音 | 演奏者明确触发 |
| 底音持续播放 | 系统自动维持 |
| 每阶段的总体强度与密度 | TouchOSC |
| 切片的具体起点和细微差异 | 受约束随机系统 |
| 第二阶段空间和音色扰动 | 系统内部受约束运动；第一版不提供 X/Y 手势控制 |
| 音量上限、反馈安全和异常停止 | 系统强制 |

## 7. 一周制作计划

### 第 1 天：锁定脚本

- 选择固定测试句子并准备一条清楚的预录版本。
- 用现有 Patch 完成 A 区倒放和 B 区逐渐显现的试听记录。
- 确认三阶段的底音听觉目标，不追求最终音色。

### 第 2 天：第一阶段

- 制作出发／战争的最小底音。
- 将完整语音、减速和一次倒放组织成约45秒段落，并保留两处只听不操作的时间。

### 第 3 天：第二阶段

- 完成一种可靠的切片播放方式。
- 加入密度和破碎程度控制。
- 删除 X/Y 表演映射，把排练注意力集中到 TRANSFORM、DENSITY、VOICE、BED 和事件按钮。
- 重做漂流背景，并试听受控微粒方案。

### 第 4 天：第三阶段

- 完成从切片到完整句子的可听见重组过程。
- 复用 B 区的缓慢滤波显现。

### 第 5 天：场景与 TouchOSC

- 建立单页界面、三个阶段按钮、3–4 个表演推子和 2–3 个可复用打击垫。
- 检查每个推子和打击垫是否能立即产生明显听觉反馈。
- 保证换段不会爆音，不会留下失控循环。

### 第 6 天：四分钟线性排练

- 连续运行完整作品并录音。
- 删除难以操作或听不出作用的控制。
- 检查总音量和紧急静音行为。

### 第 7 天：定稿与备份

- 至少完整排练三次。
- 固定一套默认参数和异常恢复流程。
- 保存演出 Patch、TouchOSC 配置、测试语音和一份录音备份。

## 8. 第一版完成标准

- 能现场录制一句话并可靠存入 buffer。
- 能完整播放原句一次。
- 三个阶段可由 TouchOSC 明确切换。
- 每个阶段即使暂时没有语音事件，也有可控的声音连续性。
- 破碎阶段仍会偶尔出现可辨认语言。
- 重组过程不是突然切换，而是能够被听见。
- 总时长约4分钟，三次排练均不超过4分15秒。
- 有总音量限制和紧急静音。
- 第一版不依赖摄像头或 X/Y 控制，摄像头失效不会影响作品。
- 每个主要表演控件都能在约 0–500 ms 内启动可辨认的声音变化。
- TouchOSC 使用一个主要页面；演奏者不需要在多个复杂页面间寻找控件。
- Stage 切换能够自动召回对应的推子预设；RESET 能恢复可预测状态且不清除录音。
- Presentation Mode 的计时器能在 Stage 1 时从 `00:00` 启动，并由 RESET 可靠停止清零。

## 9. 当前技术工作边界

- 现阶段继续用最小 Patch 验证每个声音行为，再整合演出系统。
- 新增、删除、移动对象，修改布局、属性和连线：直接编辑 `.maxpat` 文件并验证。
- Max MCP：只读取运行时信息、DSP／播放状态和调整已有参数。
- 禁止用 MCP 的 `max_save` 重建现有 Patch。
- 同一 Max 实例只允许一个 `p mcp_server` 监听 UDP 7400；演出时使用主 Patch 内嵌的 MCP，不同时打开独立 `mcp_host.maxpat` 或其他带 MCP 的 Patch。
- 所有反馈、共振、多声部叠加和总输出必须有安全音量上限。
- 第一版取消 X/Y 与摄像头声音映射；未来版本若恢复，必须重新通过可听反馈测试后才能进入演出界面。

### 9.1 对象命名规范（已确定）

- 每一个 Max 对象都必须设置唯一且稳定的 **Scripting Name**（`.maxpat` 中的 `varname`），包括音频对象、消息、按钮、数值框、注释、发送接收对象和子 Patch。
- 不允许保留无意义的自动名称作为长期名称，例如 `obj-17`、`number1` 或 `button3`。
- 名称使用简短的英文 `snake_case`，表达“所属模块＋职责”，不使用空格和中文。
- 建议前缀：`rec_` 录音、`s1_` 第一阶段、`s2_` 第二阶段、`s3_` 第三阶段、`ui_` 界面、`osc_` 通信、`gesture_` 手势、`out_` 输出、`sys_` 系统、`label_` 注释。
- 示例：`rec_start_button`、`s2_slice_density`、`gesture_x_slider`、`out_master_gain`。
- 对象功能不变时不要随意改名，使 MCP、OSC、调试记录和文档能够持续引用同一个对象。

### 9.2 参数与控制接口

- 每个需要演奏者调整的参数必须有：Scripting Name、中文说明、单位、有效范围、默认值和可听作用。
- 连续表演参数在进入声音模块前尽量标准化为 `0.0–1.0`，再由模块内部映射到 Hz、ms、增益等实际范围。
- TouchOSC 推子与 Max 测试推子应进入同一套命名控制入口，不复制声音处理链。
- OSC 地址使用稳定层级，例如 `/nostos/stage/1`、`/nostos/param/density`。
- 关键默认值通过有名字的 `loadmess` 或初始化模块设置，不依赖上一次关闭 Patch 时偶然留下的界面状态。
- 避免隐藏的“魔法数字”；重要范围和映射应在 Patch 中有注释。

### 9.3 模块与布局

- 实验 Patch 可以保持扁平，但必须按功能分区；最终演出 Patch 再整理为录音、三个阶段、OSC、场景控制和总输出等清楚模块。
- 正式演出系统必须保持一个主 Patch、一个共享语音 buffer 和一条总输出链；三个阶段是主 Patch 内部模块，不是演出时分别打开的文件。
- 三个阶段的输出使用独立命名的场景增益平滑淡入淡出；场景切换不得通过打开／关闭 Patch 窗口实现。
- 信号流尽量从左到右或从上到下；对象和注释不得重叠，相关控件靠近其声音模块。
- 工程视图固定采用“从上到下等于演出流程”的单页学习布局，横向尽量控制在约 `2200–2300 px` 内，不把后续新增模块简单追加到远端。
- 三个 Stage 统一使用四列阅读泳道：`①场景／事件控制 → ②参数映射／时间行为 → ③声音生成／处理 → ④混合／场景输出`。连续 BED 与一次性 BED PARTICLE 放在主语音链下方，避免与前景处理混在同一行。
- 三个 Stage 的主要动作／参数入口必须在对象附近使用蓝色中文 `入口：` 注释；关键播放器、粒子器、滤波器、声音源和混合节点使用灰蓝色 `核心处理：`、`声音源：` 或 `汇合点：` 注释。它们用于学习信号流，不替代紫色 `控:` 演出标记。
- 阶段内部对象必须留出足够的横纵间距，使入口注释、对象和连线可分别辨认；扩大间距时不得改变对象名称、参数、连接关系或 Presentation 坐标。
- 第 2 区按四层排列：场景切换与宏观参数、演出／安全遥控、Stage 预设、演出计时。预设和计时器不得再放到需要横向滚动才能找到的位置。
- 每个效果模块应有可控输入／输出增益，并尽量提供 bypass 或安全停止方式，便于排练时快速定位问题。
- 所有语音播放器必须明确引用同一个命名 buffer，避免出现来源不明的重复录音副本。
- 所有演出时需要演奏者操作的组件，其说明文字必须以 `控:` 开头，并使用比周围普通说明大一号的紫色粗体；状态显示、内部算法说明和信号链注释不使用该样式，使演出控件能被快速辨认。
- 工程视图第 2 区底部必须集中提供 EVENT、BED PARTICLE、MASTER、BED、AUTO VOICE、MUTE、DSP 和 RESET：声音／事件控制排在第一行，安全控制排在第二行；遥控只发送控制值，底部完整输出安全链仍然保留并可阅读。
- TouchOSC 的 UDP、解析和地址路由属于低频配置模块，统一放在工程视图最下方；上方只保留演出和调试时需要随时操作的控件。
- Stage 预设统一使用 `TRANSFORM / DENSITY / VOICE / HOLD / BED / AUTO VOICE` 六项顺序，先写入原有 UI 控件，再由 UI 的标准化链进入声音模块；X/Y 已删除。
- AUTO VOICE只控制自动前景：Stage 1和3以50 ms门控连续循环，重新开启时从loop起点开始；Stage 2停止／恢复自动粒子 metro。手动EVENT路径不经过该自动开关，RETURN也不受其限制。
- BED只控制各 Stage 的背景支路及 BED PARTICLE：Stage 1脉冲、Stage 2破碎金属、Stage 3风沙归返坐标；所有背景仍经过场景gain和总输出安全链。
- 演出计时器通过 `r nostos_scene → sel 1` 启动，以 `qmetro` 周期读取 `timer` 的实际经过时间并格式化为 `MM:SS`；它跨 Stage 持续运行，`r ctl_reset` 负责停止和清零。

### 9.4 音频与现场安全

- 所有声音最终只经过一条总输出链进入扬声器；总链必须包含主增益、音量表、安全限制和紧急静音。
- 第一版不设 PAUSE／CONTINUE 支路。唯一静音机制是限制器之后、扬声器之前的最终 MUTE；MUTE 只让输出静音，不停止内部场景、事件或计时器。
- 启停、换段、bypass 和突然改变增益时使用短斜坡或交叉淡化，避免爆音；一般从约 `20–100 ms` 开始测试。
- 反馈、共振、叠加声部和随机密度必须有限幅；任何表演控件都不能绕过安全上限。
- 每个场景切换都必须有可预测的停止行为，不能留下无法关闭的循环、延迟或持续包络。
- OSC 数据丢失时保持最后安全值片刻，然后回落到中性值；不得产生突跳或满音量。

### 9.5 每次修改后的检查

- `.maxpat` JSON 能够正常解析。
- 所有对象都有 Scripting Name，且没有重复名称。
- 所有连线引用的对象和 inlet／outlet 均存在。
- 没有对象或长注释互相遮挡。
- 新增参数具有说明、范围和默认值。
- 新增音频路径经过模块增益和总输出安全链。
- 重新打开 Patch 后功能仍然存在；必要时用 MCP 只读检查运行时对象名称、DSP 状态和连接情况。
- 结构修改不得调用 MCP `max_save`。

### 9.6 运行时验证记录（2026-09-01）

- 已在 RESET 后重新现场录音，并由 MCP 通过对象 Scripting Name 操作第一阶段 UI 完成完整测试。
- 已验证：`BED` 与 `AUTO VOICE` 可独立控制；关闭 AUTO 后背景继续；AUTO 关闭时 `EVENT B` 仍可单次召回录音；`BED = 0` 只移除背景；恢复两项控制后两层声音均恢复。
- 主 Patch 内嵌 MCP 的 `/send` 路径必须使用 `tosymbol → fromsymbol → prepend script send`，以把 MCP 传来的数字文本恢复为 Max 数值，并按 Scripting Name 驱动现有 UI 对象。
- MCP 工具当前返回的“sent to `[r name]`”文字与实际实现不一致；本项目按 Scripting Name 使用该功能。
- 主 Patch 提供命名对象 `mcp_console_reader`（`console`）；已实测可由 MCP 向它发送 `write <临时文件路径>`，再读取该文件获得完整的真实 Max Console 内容。Python 端的 `max_console_log` 仅是 OSC 命令日志，不能替代此功能。
- 已将 maxmsp-mcp PR #4 的运行时发送、Unicode 分块和 UDP 包大小修复部署到本地MCP检出目录的 `codex/deploy-pr4-local` 分支，同时保留原有ping路由修复；部署目录的Python测试16项通过，Unicode分块测试通过。公开文档不记录个人文件系统路径。
- 重启后实测 MCP heartbeat 为 0 ms；通过 `max_send` 按 Scripting Name 将 MASTER 临时设为 `-12.5` 再恢复 `-12.0`，Max Console 无错误，确认浮点文本会被恢复为 Max 数值 atom。
- 完整主 Patch dump 仍在约 64 KB 处出现 `Unterminated string`，Max Console 同时可见两段 `/patch_chunk`；说明新版进程和更大的 UDP 包已经生效，但大型 Patch 的分块传输／重组仍有待继续修复，不能把 PR #4 视为已完成本项目的完整 dump 验收。

## 10. 尚待决定或验证

- 固定测试句子和最终现场句子库。
- 三个阶段底音的具体音色。
- 第二阶段切片的适宜长度范围。
- 清晰词语应多久出现一次，观众才不会忘记原句。
- 结尾完整原句之后是否保留微弱残片。
- Max MCP 已能收到当前 Max 实例的 heartbeat，浮点参数发送也已验证；部署 PR #4 后，本主 Patch 的完整 `dump/report` 错误位置由约 32 KB 后移到约 64 KB，但仍出现 JSON 字符串截断，完整运行时读取能力仍需继续排查。
- TouchOSC UDP链已静态修复为 `udpreceive 9000 → route /nostos/...`，不再依赖 `oscparse`；尚待在Max中重开并用iPad完成真实UDP 9000运行时验收。

## 10.1 当前实现文件

- 第一版完整学习／演出 Patch：`Nostos_Performance_v01.maxpat`。
- 可交给替演者直接执行的中等复杂度操作谱：`NOSTOS_PERFORMANCE_SCRIPT_v01.md`；包含演出前检查、四分钟时间表、逐项声音动作、正式留白、预期听觉结果和故障恢复。
- 原有声音实验 Patch 移至 `experiments/Nostos_01_divine_reverse_selector.maxpat`，用作倒放与逐渐显现效果的对照，不属于正式演出入口。
- 新 Patch 已按工程视图从上到下展开为：公共录音与共享 buffer、控制与 OSC/MCP、第一阶段、第二阶段、第三阶段、总输出安全链。
- 工程视图 v0.23 保持约 `2280 px` 最大横向跨度，并进一步扩大 Stage 1/2/3 的纵向与横向对象间距；三个阶段新增 41 条就近中文入口／核心处理说明，预设和计时器位于控制区底部，Presentation 布局保持不变。
- 新 Patch 包含单页 Presentation Mode 控件，但默认先打开工程视图，便于学习对象、参数与连线。
- 当前 Stage 预设已收束为 `TRANSFORM/DENSITY/VOICE/HOLD/BED/AUTO` 六项；X/Y 已从预设和声音路径移除。
- RESET 恢复待机和默认控制状态、停止原句播放，但明确不向录音 buffer 发送 `clear`；PAUSE／CONTINUE 已从界面、控制路由和总输出链移除。
- Presentation Mode 已加入只读演出计时器；Stage 1 是计时零点，操作谱中 Stage 2、Stage 3 和 RETURN 的时间均以该屏幕时间为准。
- 所有对象（包括 MCP 子 Patch 内对象）均设置唯一 Scripting Name；静态检查已确认 JSON 可解析、对象名无缺失或重复、连线目标存在、对象框无重叠。
- 当前验证仅属于 `.maxpat` 文件的静态结构检查。正式使用前必须在 Max 中打开文件，逐项验证录音、三个阶段、TouchOSC、MCP 只读状态、音频输出和紧急静音。

## 11. 决策记录

### 2026-09-02

- 为GitHub公开发布整理仓库：主Patch、粒子声部和TouchOSC正式布局保留在根目录以维持初学者入口与 `poly~` 相对加载；早期声音实验移入 `experiments/`，截图与界面预览移入 `docs/images/`。
- README重写为公开项目首页，加入Max与TouchOSC截图、作品结构、运行要求、快速开始、UDP双向设置、文件结构、安全提示、开发验证、致谢和许可证入口；新增MIT `LICENSE`、`THIRD_PARTY_NOTICES.md`与贡献指南。
- 公开发布只分发完整双向TouchOSC布局 `nostos_full.tosc`，不再把缺失的简化布局和空白生成模板列为当前资产；清除编辑器交换文件和本机绝对路径，并扩充 `.gitignore`。
- 修复TouchOSC中仍未更新的场景与计时反馈：不再只依赖场景切换瞬间和每秒各自发送的两个独立UDP消息，新增500 ms一次的组合transport状态心跳；TouchOSC同时保留原有单项状态路径作为兼容回退。
- TouchOSC接收脚本将场景值显式经 `tonumber` 转换，并同时更新顶部 `status_label` 与反馈区 `stage_value_label`；计时器从同一transport消息的第二个参数读取。此次只修改状态回传，不改变Max内部场景、声音或计时流程。
- 正式操作谱由过度简化版调整为中等复杂度v1.0，目标仍为约4—4分15秒；保留每段明确留白，但恢复对声音身份有价值的粒子风暴、Stage 2 HOLD、Stage 3 EVENT A与沙浪事件，以及DENSITY、VOICE、BED推子的单独表演时刻。
- 中等复杂度谱遵守“一次只做一个动作”：Stage 2依次展示密度上升、粒子风暴、8秒HOLD、语音下沉、纯背景金属事件和清晰片段召回；Stage 3先展示逐渐显现与沙浪，再执行TRANSFORM重组长动作和VOICE退场。此前“这些功能仅供实验、不属于正式演出”的决定由本条取代。
- 修复TouchOSC离散控制的OSC类型不匹配：所有按钮和阶段编号在Max端先转为整数，解决场景始终显示Stage 1、Stage 1未能启动计时器、Stage 2 HOLD无效，以及DSP把浮点直接发送给 `ezdac~` 所产生的 `doesn't understand "float"`。
- 同类保护同时覆盖EVENT、RECORD、PLAY、MUTE、RESET、AUTO、BED PARTICLE和SYNC；推子类连续控制仍保留浮点精度。反馈缓存按状态类型分别使用 `int` 或 `float`，SYNC重发当前缓存值。
- 将Max底部不便编辑的iPad IP普通消息框替换为锁定状态可输入的 `textedit`，增加“应用IP”按钮，并通过 `route text → tosymbol` 得到可靠的host符号；不再要求用户按Cmd+E解锁Patch。
- 新增完全功能版 `nostos_full.tosc`，保留已通过测试的简化版不覆盖；完全版补齐MASTER、HOLD、DSP、SYNC、场景状态、计时器、MASTER dB与反馈连接状态。
- 主Patch新增 `p touchosc_feedback`，通过UDP 9001回传12类状态；新增 `/nostos/master`、`/nostos/dsp`、`/nostos/sync` 输入地址和可配置iPad目标IP。TouchOSC程序回填控件时抑制再次发送，避免OSC反馈环。
- 确认TouchOSC双向端口约定：Send Port 9000用于向Max发送表演控制，Receive Port 9001用于Max回传状态；简化版只实现前者，完全版实现双向同步。
- 修复TouchOSC输入链的 `oscparse: No such object`：确认Max的 `udpreceive 9000` 默认直接输出兼容OSC消息，删除多余的 `oscparse → list trim → route nostos`，改为直接路由13个完整 `/nostos/...` 地址；无需额外安装Package。
- 以用户提供的TouchOSC version 6空白文件生成首个单页控制界面 `nostos.tosc`，并保留 `nostos.blank.tosc` 与可重复生成脚本；当前为输入控制原型，等待iPad实际打开和UDP 9000联调。
- TouchOSC界面遵循四分钟简化谱：大型TRANSFORM与场景按钮优先，DENSITY／VOICE／BED降为较小的排练控件；Stage按钮在本地同步场景预设。MASTER、计时器和双向状态反馈暂不进入v0.1。
- 正式演出由约5分钟收缩为约4分钟：计时器内约3分20—30秒，连同录音与首次原句播放约4分钟，排练上限为4分15秒。
- 简化操作谱只保留三次场景切换、少量能够明显听见的事件和一条Stage 3重组长动作；删除Stage 1多参数堆叠、Stage 2粒子风暴／DENSITY变化／HOLD／反复扫TRANSFORM，以及Stage 3事件叠加和同时调整多个推子的要求。被删除的动作仍保留在Patch中供声音实验，不属于正式演出必做步骤。
- 留白成为正式结构：三个Stage均安排明确的“不操作”时段，操作者不需要持续制造变化，也不补按错过的事件。
- 为 Stage 1/2/3 的动作入口、参数入口和关键声音组件增加 41 条就近中文注释，并在顶部加入颜色图例；阶段对象纵向放大约 `1.4–1.5` 倍、横向间距约放大 `1.1` 倍，声音连线和 Presentation 坐标不变。
- 中文学习注释与演出控件形成固定区分：紫色 `控:` 表示演出时需要操作，蓝色 `入口：` 表示参数／事件进入模块的位置，灰蓝色表示核心处理、声音源或汇合点。
- 为学习和调试重新布局主 Patch：不改变对象名称、参数、声音算法与连线语义，把工程视图最大横向跨度从超过 `3000 px` 收束到约 `2280 px`，并扩大各区纵向间距。
- 控制区拆为“场景／宏观参数、演出遥控、Stage预设、计时器”四层；三个 Stage 统一增加“场景事件、参数映射、声音处理、混合输出”四列阅读标记。Presentation Mode 保持原位置。
- 为简化表演，删除 PAUSE／CONTINUE 的 Presentation 与工程控件、`ctl_pause` 初始化、TouchOSC `/nostos/pause` 路由以及总线 PAUSE 增益支路。
- 左右总混音改为直接进入 MASTER；经过 `clip~` 后，只保留最终 MUTE 的 20 ms 安全斜坡，再进入 meter 与扬声器。MUTE 只作用于最终输出，不停止内部声音进程。
- Presentation Mode 增加 `MM:SS` 演出计时器：按下 Stage 1 自动从 `00:00` 开始，换到 Stage 2/3/RETURN 时继续，RESET 停止并清零。
- 操作谱的正式段落提示改用屏幕计时器基准，录音和首次原句播放保留为计时前奏，避免演奏者额外心算约30秒偏移。
- 根据试听方向，Stage 2 与 Stage 3 的 BED 明确分家：Stage 2 改为双周期脉冲激发的割裂金属共振，Stage 3 保留并强化连续风沙；不再让两段都以平滑噪声为主体。
- 同一个 `BED PARTICLE` 按钮改成场景相关事件：Stage 2 触发约 0.5 秒金属碎屑爆裂，Stage 3 触发约 1.5 秒沙浪；两条声音链均独立于录音并受各自 BED、场景增益与总安全链约束。
- Stage 2 的持续金属层和一次性金属事件都设置固定低增益及限幅；Stage 3 风沙事件由包络自动结束，RESET 会清除两段事件包络。
- `BED PARTICLE` 的初版曾是 Stage 2 约 0.8 秒的气流喷溅；同日后续试听方向已将其替换为 Stage 2 金属碎屑，并为 Stage 3 增加独立沙浪声音链。
- `BED PARTICLE` 不经过 VOICE 或 AUTO VOICE，但进入 Stage 2 的 BED 汇合点，因此仍受 BED、场景淡化、MASTER、MUTE 和总输出 limiter 控制；RESET 会立即清除其包络。
- TouchOSC 增加 `/nostos/bed_particle 1`；操作谱在 Stage 2 纯背景段安排两次触发，形成可表演的背景空隙事件。
- 选择粒子方案 A：Stage 2 改为四声部受控微粒，主 Patch 通过 `poly~` 自动加载 `Nostos_GrainVoice.maxpat`，不增加新的表演控件。
- `DENSITY` 的自动粒子间隔扩大到约 `1600–70 ms`，`TRANSFORM` 的粒子长度扩大到约 `750–45 ms` 并同时控制滤波暗度；三阶段 `VOICE` 均改为从完全无声到明显前景的宽动态映射。
- `EVENT A` 在 Stage 2 触发一次约 1.2 秒的粒子风暴；`EVENT B` 和 HOLD 的既有职责保持不变。
- v0.17 曾把 Stage 2 BED 改为运动的漂流气流；此方案已在 v0.19 被割裂金属底层取代。Stage 3 继续发展轻微拍频、DENSITY 呼吸和风沙噪声。
- X/Y 控件、声音映射和八项预设旧结构已实际删除；第一版界面只保留 TRANSFORM、DENSITY、VOICE、BED 和事件／安全控制。
- 以上改动已完成静态结构验收，但“变化是否足够明显”和实际音量平衡仍须在 Max 中重开、录音并试听后确认。

### 2026-09-01

- 生成首个单文件学习／演出原型 `Nostos_Performance_v01.maxpat`，保留旧实验 Patch，不覆盖既有成果。
- 原型采用一个共享语音 buffer、三个纵向展开的场景模块和一条总输出安全链，并内置 MCP host 子 Patch。
- 工程视图保留完整中文注释、参数范围与信号流；Presentation Mode 只保留演出必需控件。
- 文件结构修改继续只通过直接编辑 `.maxpat` 完成；Max MCP 仅在文件于 Max 中打开后读取运行状态、确认对象和调整已有参数。
- 首轮静态验收通过；现场声音和交互行为仍以 Max 内实际试听为最终判断。
- 确定演出控件的统一视觉标记：说明文字以 `控:` 开头，字号比普通说明大一号，使用紫色粗体；当前主 Patch 已按此规范更新。
- 在工程视图第 2 区底部集中排列演出与调试遥控：第一行放 EVENT、MASTER、BED、AUTO VOICE，第二行放 MUTE、DSP、PAUSE、RESET；MASTER 以 dB 数值远程控制底部 `live.gain~`，MUTE 复用 `ctl_mute`，底部输出链保持完整。
- 将 TouchOSC 配置和路由整体移动到工程视图最下方，并增加 `/nostos/pause` 与 `/nostos/reset`。
- 增加 PAUSE / CONTINUE 总线软暂停和 RESET；RESET 不清除现场录音。
- 确定 Stage 切换自动召回场景预设，初始值同步回推子；MASTER、MUTE、DSP、PAUSE 和录音不随普通 Stage 切换改变。
- 建立可复现的五分钟表演操作谱：任何操作者只需识别 `控:` 标记、跟随计时器和逐时提示即可完成演出；正式演出前至少完整排练三次。
- 将声音结构明确分为BED背景、AUTO VOICE自动前景和EVENT手动前景。增加独立BED推子与AUTO VOICE开关；关闭自动语音时背景及手动事件继续，RETURN始终可以完整返回原句。
- TouchOSC新增 `/nostos/bed`（0.0–1.0）和 `/nostos/auto`（0／1），两者先同步Max界面再进入声音模块。
- 将 maxmsp-mcp PR #4 部署到当前 Codex 注册目录并完成重启验证；确认 Scripting Name 浮点参数发送恢复正常，但大型主 Patch 的 Unicode／分块 dump 仍在约 64 KB 处截断，后续需作为独立缺陷继续修复。
- 根据首次完整试听，确定第一版移除 X/Y 声像与暗亮控制；确认 DENSITY 在三个 Stage 中作用不一致、VOICE 动态对比不足，下一版必须扩大其可听范围。
- 确定重做 Stage 2／3 背景：Stage 2 使用缓慢运动的窄带噪声形成漂流气流，Stage 3 使用会呼吸并逐渐稳定的归返坐标，同时让残余噪声随重组退去。
- 粒子效果进入待确认方案，推荐使用约 4 个重叠声部的受控微粒，并复用 TRANSFORM、DENSITY、VOICE 与 EVENT A，不增加 TouchOSC 控件。

### 2026-08-31

- 将第一版总时长从约 6–8 分钟收缩到不超过 5 分钟。
- 不再按三界分割或并行处理语音，整部作品只使用同一句录音。
- 将结构收束为出发／战争、破碎／漂流、重组／归返三个阶段。
- 每个阶段使用一层持续底音，并在其上加入完整语音或切片变形。
- TouchOSC 是主要表演控制；当时暂定手势作为第二阶段的可选非关键控制，此决定已被 2026-09-01 的首次试听结果取代。
- 可表演性优先于参数数量：推子、打击垫和手的位置必须快速产生明显听觉结果。
- TouchOSC 第一版采用单一主页面，并复用少量推子和打击垫；较长渐变主要交给场景自动运行。
- 当时曾计划把手的位置等价为两个连续推子；此 X/Y 方案已在 2026-09-01 首次试听后取消。
- 所有 Max 对象必须设置唯一、稳定、语义化的 Scripting Name，保证 MCP、OSC 和人工调试能够可靠识别。
- 建立统一的参数接口、布局、音频安全和修改后检查规范，作为以后所有 Patch 的技术验收条件。
- 正式演出采用一个主 Patch：三个内部场景共享一个语音 buffer，通过独立场景增益交叉淡化，并汇合到唯一总输出链；工程视图纵向分区，演出使用单页 Presentation Mode。
