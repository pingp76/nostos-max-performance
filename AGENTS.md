# Nostos 项目协作说明

## 开始工作前

- 每次处理本项目时，必须先阅读 `NOSTOS_WORKING_DOCUMENT.md`。
- 该文件是项目范围、当前结构、已确认声音结果、控制分工和一周计划的单一事实来源。
- 涉及演出顺序、时间点、操作者动作或故障恢复时，还必须参考 `NOSTOS_PERFORMANCE_SCRIPT_v01.md`；若表演流程发生改变，应同步更新该操作谱。
- 如用户的新要求与工作文档冲突，以用户最新要求为准，并同步修订工作文档。

## 持续更新规则

- 当用户确认新的阶段性共识、否定旧方案或确定关键参数后，应在同一轮工作中更新 `NOSTOS_WORKING_DOCUMENT.md`。
- 使用“已确定／暂定／待验证”的语义区分决定成熟度；不要把建议自动写成已经确认的决定。
- 重要结构变化应追加到工作文档的“决策记录”，注明日期。
- 不要在多个零散文件中维护相互竞争的项目说明。

## 当前实现边界

- 第一版使用 Max/MSP，不使用 Faust，总时长不超过 5 分钟。
- 优先完成简单、稳定、可排练的系统，不主动扩大为三界并行架构。
- 正式演出只使用一个主 Patch。三个阶段作为主 Patch 内部模块，共享一个语音 buffer，通过独立场景增益平滑切换并汇合到唯一总输出链；不得把演出设计成需要切换三个 Patch 文件或窗口。
- 设计任何声音效果时必须先考虑可表演性：主要推子、打击垫和手势操作应快速产生明显、可重复的听觉结果，避免长时间操作后变化仍不明显。
- TouchOSC 第一版保持单一主页面，优先复用少量宏观推子和打击垫，不为每个底层参数创建独立组件。
- 将手的位置输入抽象为两个标准化连续参数 X/Y。声音设计阶段必须先用两个普通推子实现并验证，之后只替换输入源为摄像头坐标，同时保留推子作为回退控制。
- 修改 Max 对象、属性、连线或布局时，直接编辑 `.maxpat` 文件并验证。
- Max MCP 仅用于读取运行时状态、诊断和调整已有参数；不得使用 `max_save` 重建现有 Patch。
- 同一 Max 实例只保留一个监听 UDP 7400 的 `p mcp_server`；演出主 Patch 已内嵌 MCP 时，不要同时打开独立 `mcp_host.maxpat` 或其他带 MCP 的 Patch。
- 读取真实 Max Console 时，使用主 Patch 中的 `mcp_console_reader` 写入临时文件后读取；不要把 MCP 的服务器端 `max_console_log` 误当成 Max Console。
- 手势输入不得承担录音、换段、紧急静音或最终回归等关键操作。

## Max Patch 技术规范

- 每一个 Max 对象都必须有唯一且稳定的 Scripting Name（`varname`），包括音频对象、UI、消息、注释、send／receive 和子 Patch；使用语义化英文 `snake_case`。
- 名称应包含模块和职责，优先使用 `rec_`、`s1_`、`s2_`、`s3_`、`ui_`、`osc_`、`gesture_`、`out_`、`sys_`、`label_` 前缀；不得长期保留 `obj-N` 等自动名称。
- 每个可调参数都要标注含义、单位、有效范围和默认值；连续控制优先标准化为 `0.0–1.0` 后再映射。
- TouchOSC、测试推子与手势应复用同一命名参数入口。OSC 地址统一使用 `/nostos/...` 层级。
- Patch 按模块清楚布局，对象和注释不得重叠；效果模块应有增益和安全停止或 bypass。
- 所有音频必须经过唯一的总输出安全链，包括主增益、音量表、限制和紧急静音；场景切换使用短斜坡避免爆音。
- 每次修改后验证 JSON、Scripting Name 完整且唯一、连线端点、布局、参数说明、默认值及安全输出链；必要时再用 MCP 做只读运行时核对。
