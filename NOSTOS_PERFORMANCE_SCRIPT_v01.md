# 《Nostos：一句话的返航》表演操作谱 v1.0

> 适用文件：`Nostos_Performance_v01.maxpat`
> 目标时长：约 4分钟（包含录音与首次原句播放）
> 操作者要求：不需要 Max/MSP 或音乐基础，只需按时间执行按钮和推子动作。
> 核心原则：紫色粗体并以 `控:` 开头的说明，表示演出时可以操作。
> 文件要求：`Nostos_Performance_v01.maxpat` 与 `Nostos_GrainVoice.maxpat` 必须在同一文件夹；只打开前者，后者由系统自动加载。

## 1. 演出使用的控件

| 控件 | 作用 | 操作方式 |
| --- | --- | --- |
| RECORD | 开始／停止录制一句话 | 按一次开始，再按一次停止 |
| PLAY ORIGINAL | 完整播放刚录下的原句 | 点击一次 |
| STAGE 1 | 出发／战争 | 点击一次，自动载入 Stage 1 预设 |
| STAGE 2 | 破碎／漂流 | 点击一次，自动载入 Stage 2 预设 |
| STAGE 3 | 重组／归返 | 点击一次，自动载入 Stage 3 预设 |
| RETURN | 最终完整原句返回 | 点击一次；场景淡出后自动播放原句 |
| TRANSFORM | 语言变形程度 | 0最清晰，100最强变形 |
| DENSITY | Stage 1脉动／Stage 2粒子速率／Stage 3背景呼吸 | 0最稀疏或最慢，100最密集或最快 |
| VOICE | 语音与粒子存在感 | 0只剩背景，100语音／粒子明显位于前景 |
| BED | 当前Stage的背景音量 | 0无背景，100背景最突出 |
| AUTO VOICE | 自动语音流开关 | 1自动循环／粒子，0只保留背景与手动EVENT |
| EVENT A | 当前 Stage 的主要强事件 | 点击后立即产生声音事件 |
| EVENT B | 当前 Stage 的对比事件 | 点击后立即产生声音事件 |
| HOLD | 维持短残片或重复状态 | 按下开启，再按一次关闭 |
| BED PARTICLE | 不含人声的场景背景事件 | Stage 2为约0.5秒金属爆裂；Stage 3为约1.5秒沙浪 |
| RESET | 回到安全待机状态 | 点击一次；不会清除录音 |
| 演出计时 | 显示从Stage 1开始的经过时间 | 只读；Stage 1自动开始，RESET停止并清零 |
| MASTER | 总音量 | 默认约 -12 dB |
| MUTE | 紧急静音 | 1静音，0恢复 |
| DSP扬声器 | 开启／关闭声音系统 | 亮起表示开启 |

## 2. 演出前准备（观众入场前）

按以下顺序执行：

1. 打开 `Nostos_Performance_v01.maxpat`。
2. 工程视图按演出流程从上到下排列。第 2 区依次是场景／宏观参数、集中演出遥控、Stage预设和计时器；正式操作集中在带紫色 `控:` 标记的区域，不需要横向寻找远端对象，也不需要滚到最下方输出区。
3. 点击 **RESET**。
4. 确认：
   - 当前场景显示 `0`。
   - MUTE 为 `0`。
   - HOLD 为 `0`。
   - BED 为 `50`左右。
   - AUTO VOICE 为 `1`。
   - MASTER 约为 `-12 dB`。
5. 点击上方 DSP 扬声器，使其亮起。
6. 对麦克风说一句测试语，观察输入电平表有反应。
7. 短暂录音并点击 PLAY ORIGINAL，确认左右扬声器都有声音。
8. 再点击 RESET。测试录音可以保留；正式演出会被下一次录音覆盖。
9. 确认 Presentation Mode 的演出计时器显示 `00:00`；它会在点击 Stage 1 时自动开始。
10. 如果使用 `nostos_full.tosc`：确认Connection 1的Send Port为9000、Receive Port为9001；在Max最底部的文本框直接输入iPad IP，按Enter或点击“应用IP”，然后按一次 `SYNC FROM MAX`。TouchOSC显示 `MAX FEEDBACK · ONLINE` 后才算双向连接完成。

### 音量检查

- 正常情况下 MASTER 保持约 `-12 dB`。
- 如果扬声器明显过响，先把 MASTER 降到 `-18 dB`，不要修改内部增益。
- 如果出现尖锐、失控或无法判断的声音，立即按 MUTE。

### 新声音系统快速校准（排练时做，正式演出前可跳过）

先录一条4—8秒测试语，再进入 **STAGE 2**。每项只听约3—5秒：

1. 把 BED 暂时调到35、VOICE调到75、AUTO VOICE保持1。
2. DENSITY 从10推到90：人声应从约每1.5秒一个粒子变成快速粒子云；底层金属撞击也应明显变密。
3. TRANSFORM 从20推到90：应从较长、较可辨认的残片，明显变成很短且较暗的碎片。
4. VOICE 从100推到0：语音粒子必须完全退去，只剩间歇、硬质的金属底层；再恢复到75。
5. 点击 EVENT A：应立即开始一次约1.2秒的粒子风暴，不需要等待自动计时器。
6. 把 BED 调到60、VOICE调到0、AUTO VOICE调到0，再点击 BED PARTICLE：Stage 2 应只出现约0.5秒的金属碎屑爆裂；连续点两次也不应失控。
7. 进入 STAGE 3，把 TRANSFORM 从85推到0：残余摩擦应退去，但宽频风沙仍保留；把 DENSITY 从80推到10，背景呼吸应明显变慢。
8. 在 Stage 3 点击 BED PARTICLE：应听见约1.5秒的沙浪掠过，不能像 Stage 2 的金属爆裂。
9. 点击 RESET，重新准备正式演出。

如果第2—5项完全没有粒子，先确认两个 `.maxpat` 文件位于同一文件夹，再重新打开主 Patch；不要临时修改内部连线。

## 3. 观众朗读要求

- 邀请一位观众读一句话。
- 建议句子长度为 **4—8秒**，最多不要超过15秒。
- 请观众距离麦克风约15—30厘米，用正常速度、清楚地读一次。
- 朗读结束后留约1秒安静，再停止录音。
- 演出过程中不再重新录音。

## 4. 四分钟中等复杂度操作谱

整场主线仍然是：**录音与原句 → Stage 1 → Stage 2 → Stage 3 → RETURN**。每个阶段只增加一组有明确听觉目的的操作；保留留白，不要求一直移动控件。

屏幕计时器从点击 **STAGE 1** 时开始。计时器内约3分40秒，加上计时前的观众朗读与首次播放，整场约4—4分15秒。时间允许有约5秒误差，不需要追赶错过的事件。

### 计时前奏｜录音与记忆（约20—30秒）

1. 按 **RECORD**，请观众朗读一句4—8秒的话。
2. 朗读结束约1秒后再按 **RECORD**，停止录音。
3. 按 **PLAY ORIGINAL**，完整播放一次。
4. 原句结束后保持约2秒安静，再进入 Stage 1。

如果录音完全无声或严重削波，只在这里重新录制一次。进入 Stage 1 后不再重录。

### 屏幕 0:00—0:50｜Stage 1：出发／战争

系统预设：

`TRANSFORM 25 / DENSITY 35 / VOICE 75 / HOLD 0 / BED 55 / AUTO VOICE 1`

| 时间 | 必做动作 | 听觉目的 |
| --- | --- | --- |
| 0:00 | 点击 **STAGE 1** | 完整语言开始离开说话者；计时器启动 |
| 0:00—0:10 | **不操作** | 先让观众听清循环语言和底层脉动 |
| 0:10—0:25 | TRANSFORM 从25缓慢推到55 | 语言逐渐减速、降调并失去稳定方向 |
| 0:28 | 点击 **EVENT A** 一次 | 出现一次明确的倒放冲击 |
| 0:32—0:42 | DENSITY 从35推到60 | 脉动和行动密度上升，形成战争推进感 |
| 0:42—0:50 | **不操作** | 让冲突自行延续，不继续堆叠事件 |

Stage 1 不操作 EVENT B、VOICE、BED或AUTO VOICE。只用两条推子和一次倒放建立“离开”。

### 屏幕 0:50—2:05｜Stage 2：破碎／漂流

系统预设：

`TRANSFORM 70 / DENSITY 55 / VOICE 70 / HOLD 0 / BED 45 / AUTO VOICE 1`

| 时间 | 必做动作 | 听觉目的 |
| --- | --- | --- |
| 0:50 | 点击 **STAGE 2** | 语言裂成粒子，金属底层出现；参数自动回到Stage 2预设 |
| 0:50—1:02 | **不操作** | 先辨认自动粒子和割裂金属的基本状态 |
| 1:02—1:12 | DENSITY 从55推到85 | 零散碎片聚成快速粒子云，金属事件同时变密 |
| 1:14 | 点击 **EVENT A** 一次 | 触发约1.2秒的明显粒子风暴 |
| 1:22 | 点击 **HOLD** 开启 | 把短残片暂时困在重复状态 |
| 1:30 | 再点击 **HOLD** 关闭 | 释放被冻结的时间；HOLD总共保持约8秒 |
| 1:32—1:42 | VOICE 从70降到25 | 人声逐渐沉入金属背景，而不是突然消失 |
| 1:44 | AUTO VOICE 切到0 | 停止自动语音，建立纯背景空间 |
| 1:44—1:50 | **不操作** | 听见没有人声的破碎金属留白 |
| 1:50 | 点击 **BED PARTICLE** 一次 | 在纯背景中打出一次短促金属碎屑爆裂 |
| 1:57 | 点击 **EVENT B** 一次 | 从碎裂世界召回一个较清楚的语言片段 |
| 1:57—2:05 | **不操作** | 让清楚片段再次沉没 |

HOLD是开关：点击一次开启，约8秒后再点击一次关闭，不要一直按住屏幕。进入Stage 3会自动载入新预设，无需在Stage 2末尾恢复推子或AUTO VOICE。

### 屏幕 2:05—3:25｜Stage 3：重组／归返

系统预设：

`TRANSFORM 85 / DENSITY 30 / VOICE 80 / HOLD 0 / BED 40 / AUTO VOICE 1`

| 时间 | 必做动作 | 听觉目的 |
| --- | --- | --- |
| 2:05 | 点击 **STAGE 3** | 遮蔽的连续语言和风沙背景出现；参数自动回到Stage 3预设 |
| 2:05—2:17 | **不操作** | 先听见“尚未恢复”的风沙状态 |
| 2:18 | 点击 **EVENT A** 一次 | 触发约3秒的逐渐显现，第一次预示语言将回来 |
| 2:25 | 点击 **BED PARTICLE** 一次 | 一阵约1.5秒的沙浪掠过，与Stage 2金属爆裂形成对比 |
| 2:32—2:42 | BED 从40推到60 | 风沙短暂靠近，形成重组前的遮蔽高点 |
| 2:42—3:10 | TRANSFORM 从85持续、缓慢推到0 | 完成语速、亮度和语言轮廓的主要恢复过程 |
| 3:10—3:18 | VOICE 从80缓慢降到35 | 重组后的语音退后，为完整原句让出前景 |
| 3:18 | AUTO VOICE 切到0 | 自动语音退出，只剩归返背景 |
| 3:18—3:25 | **不操作** | 保留最终入口，不再触发新事件 |

Stage 3不使用HOLD或EVENT B。EVENT A、沙浪和BED动作发生在TRANSFORM长动作之前，避免操作者同时调整多个控件。

### 屏幕 3:25—约3:45｜RETURN：完整返回

| 时间 | 必做动作 | 听觉目的 |
| --- | --- | --- |
| 3:25 | 点击 **RETURN** 一次 | Stage淡出，完整原句返回 |
| 原句播放期间 | **不操作** | 让原句成为唯一焦点 |
| 原句结束后 | 保持约3秒安静 | 让结尾成立 |
| 最后约8秒 | MASTER 缓慢降到 -40 dB以下 | 余波自然退出 |
| 完全安静后 | 按 MUTE，再关闭 DSP | 演出结束 |

观众句子为4—8秒时，计时器通常在约3:40—3:50结束；加上计时前奏，整场约4—4分15秒。

## 5. MUTE 与 RESET 的使用方法

MUTE 是唯一的静音控制，位于最终扬声器输出之前：

- 临时需要处理现场问题时，把 MUTE 切到 `1`；最终输出会快速平滑静音。
- 内部场景、循环、随机事件和屏幕计时器仍然继续运行。
- 问题解决后把 MUTE 切回 `0`，按照计时器当前时间继续；不要补按已经错过的 EVENT。
- 如果中断超过约30秒或已经不知道当前状态，保持 MUTE，按 RESET 后重新开始；录音仍然保留。

RESET 用于演出开始前、排练重新开始或出现逻辑混乱时。

按下 RESET 后：

- 当前场景回到0待机。
- TRANSFORM、DENSITY、VOICE、HOLD恢复默认值。
- BED恢复为50，AUTO VOICE恢复为1。
- MASTER恢复约-12 dB。
- MUTE恢复为0。
- 正在播放的完整原句停止。
- 演出计时器停止并清零为 `00:00`。
- DSP保持当前状态。
- 已录制的一句话继续保留，不会被清除。

RESET 不是演出中的常规动作。正常表演只在开始前使用；发生故障时再使用。

## 6. 故障处理

### 突然过响、尖锐或失控

1. 立即按 MUTE。
2. 把 MASTER 降到 `-30 dB` 以下。
3. 按 RESET。
4. 确认没有异常后解除 MUTE，把 MASTER慢慢恢复到安全值。

### 完全没有声音

按顺序检查：

1. DSP扬声器是否亮起。
2. MUTE是否为0。
3. MASTER是否在约-12 dB附近。
4. 当前场景是否为1、2或3；场景0本来就是待机静音。
5. 点击 PLAY ORIGINAL，判断录音是否存在。

### TouchOSC失效

- 不要中止演出。
- 直接使用 Max 上方带 `控:` 的按钮和推子继续。
- 不要在演出中修改最下方的UDP或路由对象。
- 排练后检查TouchOSC Connection 1：Host为Mac局域网IP，Send Port为9000，Receive Port为9001；不要把两个方向互换。
- 如果TouchOSC仍能控制Max、但场景和计时器不回传，说明9000输入正常而9001返回失败：重新确认Max底部的iPad IP并按一次 `SYNC FROM MAX`。

### 操作者忘记当前进行到哪里

- 查看计时器和“当前场景”数字。
- 如果只错过一个 EVENT，跳过它，不要补按。
- 如果 Stage 明显错误，直接点击正确 Stage；系统会召回该 Stage 的初始预设。

## 7. 一页速查表

| 时间 | 必做动作 |
| --- | --- |
| 演前 | RESET → MASTER -12 dB → MUTE 0 → DSP开启 |
| 计时前 | RECORD录音 → PLAY ORIGINAL一次 → 留白2秒 |
| 0:00 | STAGE 1 |
| 0:10—0:42 | TRANSFORM 25→55 → EVENT A → DENSITY 35→60 |
| 0:50 | STAGE 2 |
| 1:02—1:30 | DENSITY 55→85 → EVENT A粒子风暴 → HOLD开／关 |
| 1:32—1:57 | VOICE 70→25 → AUTO 0 → BED PARTICLE → EVENT B |
| 2:05 | STAGE 3 |
| 2:18—2:42 | EVENT A → BED PARTICLE沙浪 → BED 40→60 |
| 2:42—3:18 | TRANSFORM 85→0 → VOICE 80→35 → AUTO 0 |
| 3:25 | RETURN，随后不操作 |
| 原句结束 | 等3秒 → MASTER缓慢下降 → MUTE → DSP关闭 |

## 8. 排练验收

正式演出前至少完整执行三次，并确认：

- 三次总时长都约为4分钟，并且不超过4分15秒。
- 操作者不看内部连线也能完成全部动作。
- 每次点击 Stage 后推子都会回到对应预设。
- EVENT A、EVENT B和HOLD不会造成不可控音量。
- BED PARTICLE在VOICE和AUTO VOICE均为0时仍清楚可辨；Stage 2金属爆裂和Stage 3沙浪能够仅凭听觉区分，连续点击不会造成削波或残留声音。
- MUTE和RESET都能可靠工作；MUTE时扬声器无声，但内部场景与计时器继续运行。
- Stage 1 会把屏幕计时器从 `00:00` 启动；Stage 2/3 不重置；RESET 会停止并清零。
- RETURN后没有误触其他声音事件。
- 完整原句在开头和结尾都清楚可辨认。
