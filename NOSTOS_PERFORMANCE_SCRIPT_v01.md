# 《Nostos：一句话的返航》表演操作谱 v0.2

> 适用文件：`Nostos_Performance_v01.maxpat`  
> 目标时长：约 4分40秒—5分钟  
> 操作者要求：不需要 Max/MSP 或音乐基础，只需按时间执行按钮和推子动作。  
> 核心原则：紫色粗体并以 `控:` 开头的说明，表示演出时可以操作。

## 1. 演出使用的控件

| 控件 | 作用 | 操作方式 |
| --- | --- | --- |
| RECORD | 开始／停止录制一句话 | 按一次开始，再按一次停止 |
| PLAY ORIGINAL | 完整播放刚录下的原句 | 点击一次 |
| STAGE 1 | 出发／战争 | 点击一次，自动载入 Stage 1 预设 |
| STAGE 2 | 破碎／漂流 | 点击一次，自动载入 Stage 2 预设 |
| STAGE 3 | 重组／归返 | 点击一次，自动载入 Stage 3 预设 |
| RETURN | 最终完整原句返回 | 点击一次；场景淡出后自动播放原句 |
| TRANSFORM | 语言变形程度 | 0最清晰，100最强变形 |
| DENSITY | 重复／切片密度 | 0最稀疏，100最密集 |
| VOICE | 语音存在感 | 0几乎无语音，100语音最突出 |
| BED | 当前Stage的背景音量 | 0无背景，100背景最突出 |
| AUTO VOICE | 自动语音流开关 | 1自动循环／切片，0只保留背景与手动EVENT |
| X | 左右位置 | 0最左，50中央，100最右 |
| Y | 明暗／亮度 | 0最暗，100最亮 |
| EVENT A | 当前 Stage 的主要强事件 | 点击后立即产生声音事件 |
| EVENT B | 当前 Stage 的对比事件 | 点击后立即产生声音事件 |
| HOLD | 维持短残片或重复状态 | 按下开启，再按一次关闭 |
| PAUSE / CONTINUE | 暂停／继续总输出 | 1暂停，0继续；使用50 ms淡变 |
| RESET | 回到安全待机状态 | 点击一次；不会清除录音 |
| MASTER | 总音量 | 默认约 -12 dB |
| MUTE | 紧急静音 | 1静音，0恢复 |
| DSP扬声器 | 开启／关闭声音系统 | 亮起表示开启 |

## 2. 演出前准备（观众入场前）

按以下顺序执行：

1. 打开 `Nostos_Performance_v01.maxpat`。
2. 使用工程视图上方的调试遥控，不需要滚到最下方输出区。
3. 点击 **RESET**。
4. 确认：
   - 当前场景显示 `0`。
   - PAUSE 为 `0`。
   - MUTE 为 `0`。
   - HOLD 为 `0`。
   - BED 为 `50`左右。
   - AUTO VOICE 为 `1`。
   - MASTER 约为 `-12 dB`。
5. 点击上方 DSP 扬声器，使其亮起。
6. 对麦克风说一句测试语，观察输入电平表有反应。
7. 短暂录音并点击 PLAY ORIGINAL，确认左右扬声器都有声音。
8. 再点击 RESET。测试录音可以保留；正式演出会被下一次录音覆盖。
9. 把计时器准备在 `0:00`，由操作者或助手负责计时。

### 音量检查

- 正常情况下 MASTER 保持约 `-12 dB`。
- 如果扬声器明显过响，先把 MASTER 降到 `-18 dB`，不要修改内部增益。
- 如果出现尖锐、失控或无法判断的声音，立即按 MUTE。

## 3. 观众朗读要求

- 邀请一位观众读一句话。
- 建议句子长度为 **4—8秒**，最多不要超过15秒。
- 请观众距离麦克风约15—30厘米，用正常速度、清楚地读一次。
- 朗读结束后留约1秒安静，再停止录音。
- 演出过程中不再重新录音。

## 4. 五分钟逐时操作谱

时间允许有约5秒偏差。除特别注明外，推子都应平稳移动，不要突然跳到目标值。

### 0:00—0:30｜录音与记忆建立

| 时间 | 操作 | 应听见什么 |
| --- | --- | --- |
| 0:00 | 按 RECORD，示意观众开始朗读 | 现场只听见观众本人 |
| 朗读结束后约1秒 | 再按 RECORD，停止录音 | 录音结束，保持短暂安静 |
| 约0:10 | 按 PLAY ORIGINAL | 完整、未经变形的原句播放一次 |
| 原句结束后 | 等待约2秒，不操作 | 观众形成对原句的记忆 |

如果原句明显没有录到、削波或完全听不清，只允许在此处重新录制一次。进入 Stage 1 后不再重录。

### 0:30—1:35｜Stage 1：出发／战争

点击 **STAGE 1**。系统自动载入：

`TRANSFORM 25 / DENSITY 35 / VOICE 75 / X 50 / Y 50 / HOLD 0 / BED 55 / AUTO VOICE 1`

| 时间 | 操作 | 应听见什么 |
| --- | --- | --- |
| 0:30 | 点击 STAGE 1 | 完整语言进入循环，底层脉动出现 |
| 0:40—0:55 | TRANSFORM 从25缓慢推到40 | 语速和时间方向开始不稳定，但仍可辨认 |
| 0:50—1:05 | DENSITY 从35推到55 | 推进感和冲突密度增加 |
| 1:05 | 点击 EVENT B 一次 | 出现一次短暂失速／受阻事件 |
| 1:15 | 点击 EVENT A 一次 | 出现明显倒放冲击，语言方向被扭转 |
| 1:20—1:30 | TRANSFORM 推到55；DENSITY推到65；BED推到65 | 冲突达到本段最高点 |
| 1:30 | AUTO VOICE切到0 | 连续语音淡出，只剩背景，形成清楚段落边界 |
| 1:32 | 停止移动，等待约3秒 | 在纯背景中准备进入下一Stage |

本段不要连续快速点击 EVENT。EVENT A 和 B 各一次即可。

### 1:35—3:10｜Stage 2：破碎／漂流

点击 **STAGE 2**。系统自动载入：

`TRANSFORM 70 / DENSITY 55 / VOICE 70 / X 50 / Y 55 / HOLD 0 / BED 45 / AUTO VOICE 1`

| 时间 | 操作 | 应听见什么 |
| --- | --- | --- |
| 1:35 | 点击 STAGE 2 | 完整句子裂成切片，空间和时间变得不稳定 |
| 1:45—2:00 | X 从50缓慢移到20，再移到80 | 切片从左侧漂向右侧 |
| 2:00 | 点击 EVENT A 一次 | 立即出现一组明显切片事件 |
| 2:10—2:25 | DENSITY 从55推到75 | 切片短时变密，形成漂流中的风暴 |
| 2:25 | AUTO VOICE切到0；BED推到60 | 自动切片停止，只留下漂流背景 |
| 2:25—2:35 | DENSITY 从75降到30 | 为重新开启自动语音准备较稀疏的密度 |
| 2:35 | 保持AUTO VOICE=0，点击EVENT B一次 | 在纯背景上单独召回一个清楚语言片段 |
| 2:43 | 开启 HOLD，保持约4秒后关闭 | 一个短残片被抓住并重复，然后释放 |
| 2:50 | AUTO VOICE切回1；BED降回45 | 自动切片重新进入，背景退回次要位置 |
| 2:50—3:00 | Y 从55降到25，再升到65 | 声音先变暗、沉没，再重新变亮 |
| 3:00 | 再点击 EVENT B 一次 | 在进入归返前再次提醒观众原句 |
| 3:05—3:10 | DENSITY保持约35；X回到50 | 漂流短暂稳定，暗示归返开始 |

如果使用摄像头手势，手的左右替代 X、上下替代 Y；其余操作不变。第一次演出建议仍使用推子。

### 3:10—4:30｜Stage 3：重组／归返

点击 **STAGE 3**。系统自动载入：

`TRANSFORM 85 / DENSITY 30 / VOICE 80 / X 50 / Y 45 / HOLD 0 / BED 40 / AUTO VOICE 1`

这一段最重要的动作是把 TRANSFORM 从高位缓慢拉回0，让“恢复秩序”能够被听见。

| 时间 | 操作 | 应听见什么 |
| --- | --- | --- |
| 3:10 | 点击 STAGE 3 | 语言仍然破碎、遮蔽，但归返过程开始 |
| 3:15—3:35 | TRANSFORM 从85缓慢降到65 | 播放速度开始接近正常，语言轮廓出现 |
| 3:35 | 点击 EVENT A 一次 | 出现一次约3秒的显现过程 |
| 3:35—3:55 | TRANSFORM 从65降到40；Y从45升到65 | 语音逐渐变亮，相邻片段关系变清楚 |
| 3:55 | 点击 EVENT B 一次 | 召回一个清晰开头，确认原句仍存在 |
| 4:00—4:15 | TRANSFORM 从40降到15；DENSITY从30降到15 | 碎片减少，语速和顺序趋于稳定 |
| 4:15—4:25 | TRANSFORM 从15缓慢降到0；VOICE推到90；BED降到20 | 语言接近完整、清楚，背景主动让出空间 |
| 4:25 | AUTO VOICE切到0 | 重组语音淡出，只留下很弱背景 |
| 4:25—4:30 | 不再操作，保持5秒 | 在背景余波中准备最终完整返回 |

不要提前按 RETURN。TRANSFORM 到0以后仍应等待几秒，让“重组完成”成为可听见的结果。

### 4:30—约4:55｜RETURN：完整返回与结束

| 时间 | 操作 | 应听见什么 |
| --- | --- | --- |
| 4:30 | 点击 RETURN 一次 | 三个 Stage 淡出；约550 ms后完整原句开始播放 |
| 原句播放期间 | 不操作任何 EVENT、Stage或推子 | 完整原句作为唯一焦点返回 |
| 原句结束后 | 保持约3秒安静 | 让结尾成立 |
| 最后5秒 | MASTER 从约-12 dB缓慢降到-40 dB以下 | 余音退去，而不是突然切断 |
| 完全安静后 | 按 MUTE，再关闭 DSP | 演出结束 |

总时长会随观众句子长度产生数秒差异，但应控制在5分钟以内。

## 5. PAUSE / CONTINUE 的使用方法

PAUSE 是演出中断工具，不是音乐效果。

- 临时需要说话、处理现场噪声或等待设备时，把 PAUSE 切到 `1`。
- 输出会在约50 ms内淡出，不会产生明显爆音。
- 内部循环和随机过程仍然继续运行。
- 恢复时把 PAUSE 切回 `0`，声音会在约50 ms内淡入。
- 短暂停顿后继续按照计时器当前时间执行，不补做暂停期间错过的 EVENT。
- 如果暂停超过约30秒，建议按 RESET，然后从当前 Stage 的开头重新开始；录音仍然保留。

## 6. RESET 的使用方法

RESET 用于演出开始前、排练重新开始或出现逻辑混乱时。

按下 RESET 后：

- 当前场景回到0待机。
- TRANSFORM、DENSITY、VOICE、X、Y、HOLD恢复默认值。
- BED恢复为50，AUTO VOICE恢复为1。
- MASTER恢复约-12 dB。
- MUTE和PAUSE恢复为0。
- 正在播放的完整原句停止。
- DSP保持当前状态。
- 已录制的一句话继续保留，不会被清除。

RESET 不是演出中的常规动作。正常表演只在开始前使用；发生故障时再使用。

## 7. 故障处理

### 突然过响、尖锐或失控

1. 立即按 MUTE。
2. 把 MASTER 降到 `-30 dB` 以下。
3. 按 RESET。
4. 确认没有异常后解除 MUTE，把 MASTER慢慢恢复到安全值。

### 完全没有声音

按顺序检查：

1. DSP扬声器是否亮起。
2. MUTE是否为0。
3. PAUSE是否为0。
4. MASTER是否在约-12 dB附近。
5. 当前场景是否为1、2或3；场景0本来就是待机静音。
6. 点击 PLAY ORIGINAL，判断录音是否存在。

### TouchOSC失效

- 不要中止演出。
- 直接使用 Max 上方带 `控:` 的按钮和推子继续。
- 不要在演出中修改最下方的UDP或路由对象。

### 摄像头手势失效

- 改用 X、Y 推子继续。
- 不需要重新开始 Stage，也不需要 RESET。

### 操作者忘记当前进行到哪里

- 查看计时器和“当前场景”数字。
- 如果只错过一个 EVENT，跳过它，不要补按。
- 如果 Stage 明显错误，直接点击正确 Stage；系统会召回该 Stage 的初始预设。

## 8. 一页速查表

| 时间 | 必做动作 |
| --- | --- |
| 演前 | RESET → MASTER -12 dB → MUTE 0 → PAUSE 0 → DSP开启 |
| 0:00 | RECORD → 观众朗读 → RECORD停止 |
| 约0:10 | PLAY ORIGINAL一次 |
| 0:30 | STAGE 1 |
| 0:40—1:30 | TRANSFORM 25→55；DENSITY 35→65；BED→65；B一次；A一次；最后AUTO 0 |
| 1:35 | STAGE 2 |
| 1:45—3:05 | X左右漂移；A一次；AUTO 0形成纯背景；B两次；HOLD约4秒；AUTO 1；Y暗→亮 |
| 3:10 | STAGE 3 |
| 3:15—4:25 | TRANSFORM 85→0；A一次；B一次；VOICE→90；BED→20；最后AUTO 0 |
| 4:30 | RETURN，随后不操作 |
| 原句结束 | 等3秒 → MASTER缓慢下降 → MUTE → DSP关闭 |

## 9. 排练验收

正式演出前至少完整执行三次，并确认：

- 三次总时长都不超过5分钟。
- 操作者不看内部连线也能完成全部动作。
- 每次点击 Stage 后推子都会回到对应预设。
- EVENT A、EVENT B和HOLD不会造成不可控音量。
- PAUSE、CONTINUE、RESET和MUTE都能可靠工作。
- RETURN后没有误触其他声音事件。
- 完整原句在开头和结尾都清楚可辨认。
